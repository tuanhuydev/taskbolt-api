server:
  port: 8083
spring:
  application:
    name: taskbolt
  datasource:
    url: jdbc:postgresql://localhost:5432/taskbolt
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
