server:
  port: 8083
spring:
  application:
    name: taskbolt
    datasource:
      url: ${DATABASE_URL:127.0.01}
      username: ${DATABASE_USERNAME:postgres}
      password: ${DATABASE_PASSWORD:postgres}
      driver-class-name: org.postgresql.Driver

